import styled from "styled-components";
import Form from "./Form";
import Todos from "./Todos";

function Home() {
    return (
        <div>
            <Form />
            <Todos />
        </div>
    );
}

export default Home;
